const { resolve } = require ('path')

module.exports = {
    config: resolve ( __dirname, 'src', 'config', 'database.js'),//arquivo de configuracao
    'models-path': resolve( __dirname, 'src', 'app', 'models'),//nossos models
    'migrations-path': resolve ( __dirname, 'src', 'database', 'migrations')//e onde estara as migrations
}